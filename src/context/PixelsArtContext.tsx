import { createContext } from 'react';
import PixelsArtContextProps from '../interfaces/context.interface';

const PixelsArtContext = createContext({} as PixelsArtContextProps);

export default PixelsArtContext;
